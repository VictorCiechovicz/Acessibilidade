import React from 'react';
import { View } from 'react-native';
import styles from './styles';

const Listar = () => {
    return (<View>
        <Text>Listar</Text>
    </View>);
}

export default Listar;